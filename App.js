/**
 * App.js (entry shim)
 * ────────────────────
 * The application now lives under `src/`. This file simply re-exports the
 * root component so the Expo entry point (index.js → registerRootComponent)
 * keeps working unchanged.
 */
export { default } from "./src/App";
