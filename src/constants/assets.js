/**
 * constants/assets.js
 * ────────────────────
 * Central registry of static assets. Keeps `require()` paths in one place
 * so they can be swapped or referenced without hunting through screens.
 */
export const mapPhoto = require("../../assets/mapphoto.png");

export default { mapPhoto };
